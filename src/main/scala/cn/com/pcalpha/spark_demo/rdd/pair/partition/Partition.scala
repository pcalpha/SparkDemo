package cn.com.pcalpha.spark_demo.rdd.pair.partition

object Partition {

  /**
    * 这里列出了所有会为生成的结果RDD设好分区方式的操作：
    * cogroup()、
    * groupWith()、
    * join()、
    * leftOuterJoin()、
    * rightOuterJoin()、
    * groupByKey()、
    * reduceByKey()、
    * combineByKey()、
    * partitionBy()、
    * sort()、
    * mapValues()（ 如果 父 RDD 有分 区 方式 的 话）、
    * flatMapValues()（ 如果 父 RDD 有分 区 方式 的 话）
    * filter()（ 如果 父 RDD 有分 区 方式 的 话）
    * @param args
    */
  def main(args: Array[String]): Unit = {

  }
}
